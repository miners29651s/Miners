# Convex deployment URL, from `npx convex dev` output
NEXT_PUBLIC_CONVEX_URL=

# Telegram bot token — used ONLY server-side (convex/lib/telegramAuth.ts) to
# validate initData. Never expose this with a NEXT_PUBLIC_ prefix.
TELEGRAM_BOT_TOKEN=
